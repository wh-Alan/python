仿写ftp
ftp服务端：
应用socketsever模块的功能
可以对应处理多用户的请求

对应在interface目录下，主要功能如下：
            ['注册',register],
            ['登录',login],
            ['切换目录',change_dir],
            ['上传',upload],
            ['下载',download],
            ['查看目录下的文件',check_file],
            ['创建文件夹',mk_dir],

传输时封装了自己的协议

用户注册时默认在db目录下新建一个以用户名的目录，传输密码时是加密状态（MD5）
上传时会改变用户信息，即是空间的大小，默认给的空间大小100M
另外上传或是下载时分别对文件进行了校验（基于md5），保证文件的一致性


用户信息：
        {
            'username':{'pwd':'123',mydir=['url1'],'mydir_size':0,'max_size':10240000},
            'username':{'pwd':'123',mydir=['url1'],'mydir_size':0,'max_size':10240000},
        }

重要:没有在服务端处理用户登录状态，只是在客户端简单处理下，
     没有做上传目录的功能，只有文件
     没有做断点续传。
     总的来说，自己的问题，没有分配好时间。
     客户端Readme有些许不一样